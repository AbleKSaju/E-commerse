<section class="vh-100" style="background-color: #eee;">
    <div class="container h-100">
      <div class="row d-flex justify-content-center align-items-center h-100">
        <div class="col-lg-12 col-xl-11">
          <div class="card text-black" style="border-radius: 25px;">
            <div class="card-body p-md-5">
              <div class="row justify-content-center">
                <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">
  
                  <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">Sign up</p>

                  <%if(exist){%>
                    <p id="passError" class="text-danger">Account Already Exist</p>
                    <%}%>
  
                  <form class="mx-1 mx-md-4" method="post" action="/signup">
                    <div class="d-flex flex-row align-items-center mb-4">
                      <!-- <i class="fas fa-user fa-lg me-3 fa-fw"></i> -->
                      <div class="form-outline flex-fill mb-0">
                        <label class="form-label"  for="form3Example1c">Your Name</label>
                        <input type="text" id="name" required="true" type="text" id="nameInput" onblur="capitalizeFirstLetter(this)" name="name"  class="form-control" />
                       
                      </div>
                    </div>
  
                      <div class="d-flex flex-row align-items-center mb-4">
                      <!-- <i class="fas fa-key fa-lg me-3 fa-fw"></i> -->
                      <div class="form-outline flex-fill mb-0">
                        <label class="form-label" for="form3Example4cd">Mobile</label>
                        <input type="text" id="number" required="true" title="must be 10 digits" pattern="[1-9]{1}[0-9]{9}" name="mobile" class="form-control" />
              
                      </div>
                    </div>
  
                    <div class="d-flex flex-row align-items-center mb-4">
                      <!-- <i class="fas fa-envelope fa-lg me-3 fa-fw"></i> -->
                      <div class="form-outline flex-fill mb-0">
                        <label class="form-label" for="form3Example3c">Your Email</label>
                        <input type="email" id="email" required="true" name="email" class="form-control" />
                      
                      </div>
                    </div>
                   
  
                    <!-- <div class="d-flex flex-row align-items-center mb-4">
                      <div class="form-outline flex-fill mb-0">
                        <input type="password" id="password" required="true" pattern=".{4,}" title="Atleast 4 digit"  name="password" class="form-control" />
                        <label class="form-label" for="form3Example4c">Password</label>
                      </div>
                    </div>
 
                    <div class="d-flex flex-row align-items-center mb-4">
                      <div class="form-outline flex-fill mb-0">
                        <input type="password" id="password" required="true" pattern=".{4,}" title="Atleast 4 digit"  name="password" class="form-control" />
                        <label class="form-label" for="form3Example4c">Repeat Password</label>
                      </div>
                    </div> -->
                    <div class="d-flex flex-row align-items-center mb-4">
                      <div class="form-outline flex-fill mb-0">
                        <label class="form-label" for="password">Password</label>
                        <input
                          type="password"
                          id="password"
                          required="true"
                          pattern=".{4,}"
                          title="At least 4 characters"
                          name="password"
                          class="form-control"
                          oninput="validatePasswords()"
                        />
                        
                      </div>
                    </div>
                    
                    <div class="d-flex flex-row align-items-center mb-4">
                      <div class="form-outline flex-fill mb-0">
                        <label class="form-label" for="repeatPassword">Repeat Password</label>
                        <input
                          type="password"
                          id="repeatPassword"
                          required="true"
                          pattern=".{4,}"
                          title="At least 4 characters"
                          name="repeatPassword"
                          class="form-control"
                          oninput="validatePasswords()"
                        />
                    
                      </div>
                    </div>
                    
                    <script>
                      function validatePasswords() {
                        var password = document.getElementById("password").value;
                        var repeatPassword = document.getElementById("repeatPassword").value;
                        var repeatPasswordInput = document.getElementById("repeatPassword");
                    
                        if (password === repeatPassword) {
                          repeatPasswordInput.style.border = ""; // Reset border
                        } else {
                          repeatPasswordInput.style.border = "1px solid red"; // Apply red border
                        }
                      }
                    </script>
                    
                    <style>
                      /* Additional CSS styles can be added here */
                    </style>
                    
                    <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
                      <button type="submit" class="btn btn-primary btn-lg">Sent OTP</button>
                    </div>
                  </form>
  
                </div>
                <!-- <div class="col-md-10 col-lg-6 col-xl-7 d-flex align-items-center order-1 order-lg-2">
  
                  <img src="/images/dps.jpeg " style="height: 400px;width: 600px;"
                    class="img-fluid" alt=""/>
  
                </div> -->
                <div class="col-md-10 col-lg-6 col-xl-7 d-flex align-items-center order-1 order-lg-2">
                  <div class="image-container">
                    <img src="/images/dps.jpeg" class="styled-image" alt="Styled Image"/>
                  </div>
                </div>
                
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <style>
    /* Custom styles for the image container */
    .image-container {
      background-color: #f0f0f0;
      padding: 20px;
      border-radius: 15px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
    }
  
    /* Styling for the image */
    .styled-image {
      max-width: 100%;
      height: auto;
      border-radius: 10px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    }
</style>
<script>
function validate(){
document.getElementById('err-msg').innerHTML = ''
let name = document.getElementById('name').value;
const nameRegex = /^[A-Z][a-z]*$/;
if(nameRegex.test(name)){
}else{  
  document.getElementById('name').style.borderColor = 'red'
  document.getElementById('err-msg').innerHTML = document.getElementById('err-msg').innerHTML + '<br> Name error'
  //alert("Name Eroor 1:Start with capital letter 2:No white spaces 3:No Special symbols")
  event.preventDefault()
}

const passwordRegex = /^(?=.[A-Za-z])(?=.\d)[A-Za-z\d]{8,}$/;
let password = document.getElementById('password').value;
if(passwordRegex.test(password)){}else{
  document.getElementById('err-msg').innerHTML = document.getElementById('err-msg').innerHTML + '<br> Password error'
  document.getElementById('password').style.borderColor = 'red'
  //alert('Password Error 1:Minimum 8 charecters 2:use charecters and number 3:no white spaces')
  event.preventDefault()
}

const phoneNumberRegex = /^\d{10}$/;
let number = document.getElementById('number').value;

if(phoneNumberRegex.test(number)){}else{
  document.getElementById('err-msg').innerHTML = document.getElementById('err-msg').innerHTML + '<br> Number error'
  document.getElementById('number').style.borderColor = 'red'
  //alert('Number Error')
}
}

setTimeout(() => {
  document.getElementById('passError').style.display = 'none';
   }, 3000);


</script>